<Window x:Class="WordSearchGenerator.Desktop.Views.Dialogs.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icons="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
        xmlns:localization="clr-namespace:WordSearchGenerator.Desktop.Localization"
        Title="{localization:Loc AboutTitle}"
        Icon="/WordSearchGenerator.Desktop;component/WordSearchGenerator.Desktop.ico"
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal">
      <icons:FluentIcon Margin="0,0,7,0"
                        VerticalAlignment="Center"
                        Icon="Info"
                        IconSize="Size24"
                        FontSize="22" />
      <TextBlock VerticalAlignment="Center"
                 Style="{StaticResource PaneTitleTextStyle}"
                 Text="{localization:Loc AboutTitle}" />
    </StackPanel>

    <TextBlock Grid.Row="1"
               Margin="24,14,0,0"
               Text="{Binding VersionText}"
               TextWrapping="Wrap" />

    <Button Grid.Row="2"
            MinWidth="80"
            Margin="0,16,0,0"
            HorizontalAlignment="Right"
            IsCancel="True"
            IsDefault="True"
            Content="{localization:Loc SettingsOk}" />
  </Grid>
</Window>
